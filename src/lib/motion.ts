/** Smooth exponential deceleration. Use for entrances, hover lifts, card reveals. */
export const EASE_OUT_EXPO = [0.22, 1, 0.36, 1] as const;

/** Standard ease-in-out. Use for stagger reveals and subtle transitions. */
export const EASE_STANDARD = [0.25, 0.46, 0.45, 0.94] as const;
